# Android Lab Platform Gateway Agent Environment Variables

# Server URL (required)
SERVER_URL=http://localhost:8000

# Gateway ID (optional, defaults to hostname)
# GATEWAY_ID=my-gateway-1

# WebSocket URL for receiving commands (optional)
# If not provided, the gateway will not accept remote commands
WEBSOCKET_URL=ws://localhost:8765

# Heartbeat interval in seconds (optional, defaults to 30)
INTERVAL=30

# Device scan interval in seconds (optional, defaults to 10)
DEVICE_SCAN_INTERVAL=10

# Serial port scan interval in seconds (optional, defaults to 30)
SERIAL_PORT_SCAN_INTERVAL=30

# Serial port baud rate (optional, defaults to 115200)
SERIAL_PORT_BAUD_RATE=115200

# Authentication credentials (optional)
# If not provided, the gateway will run without authentication
USERNAME=admin
PASSWORD=admin

# Logging level (optional, defaults to INFO)
# Valid values: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
